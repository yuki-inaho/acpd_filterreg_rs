# 是正後の再レビュー

## 判定

前回のP0（格子の欠落、twistの欠落、ACPDの別手法化）は数値核の実装と原コード成分照合で是正しました。
ただし実nanobind境界と実pixi環境は未検証のままであり、**総合受入はBLOCKED**です。未達を注意書きだけで合格へ変更しません。

| 指摘 | 是正後 | 証拠 |
|---|---|---|
| R01 実格子欠落 | 是正 | 両言語lattice、原probreg32条件、原FilterReg単体座標2条件 |
| R02 twist欠落 | 是正 | 両言語rigid、2D/3D有限差分、点対線・面を含む既知姿勢回復 |
| R03 ACPDの無断変更 | 是正 | 正則化・投影・減衰・変位上限を削除。原M-step8条件 |
| R04 次数10不可 | 是正 | 66/286項の基底、原基底12条件 |
| R05 次数割当不一致 | 是正 | 原スケジュール24条件で完全一致 |
| R06 最良復元なし | 是正 | 点群・分散・写像列を同じ最良反復へ戻す試験 |
| R07 分散継承の必須化 | 是正 | CPD式が既定、継承は明示的拡張として区別 |
| R08 自己試験だけ | 是正 | 原ソースの別ビルドと抽出ハッシュ、固定参照出力 |
| R09 境界・環境未検証 | **未達** | 依存未取得／実行形式不存在の実ログ。Rust実行免除は明示 |
| R10 複数正本の混在 | 是正 | 0.2.0一本化、互換性破壊と保存形式2を明記 |

## 是正後にも残る品質上の注意

36条件の合成監査で、剛体段階後の既知対応誤差が改善17、悪化6、不変13でした（比較許容1e-8、例外0）。
これは原論文の成功率や実データ性能の再現ではありません。失敗を除いて成功例だけ報告したり、既知対応の真値を停止に混ぜたりしていません。

格子出力一致とM-step一致は、全登録問題の正解を保証しません。対称性、非凸性、近似格子、過大な自由度、外挿、多項式の折返しには制約がありません。
残差の定数・一次項を保持するため、回転・並進変数の固定は、剛体様変位を残差から数学的に完全除去する意味ではありません。

Rustは全文と設定・試験の静的点検のみです。特にPyO3型変換、借用/GIL、nalgebraの特異値分解の挙動は実行での裏付けが必要です。
`tests/test_native_boundary.py` は実拡張だけを受け付け、driverでは失敗するようにしています。
