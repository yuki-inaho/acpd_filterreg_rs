# 是正実装・検証報告（0.2.0）

## 総合判定

**BLOCKED。全DoDの達成ではありません。** 実装前に固定した作業書の条件は変更していません。
D07（実nanobind拡張のビルドと境界試験）、D09（実pixi依存解決）は未達です。
Rust/PyO3の実行必須だけは、前のユーザー指示により免除されています。Rustのアルゴリズムや接続コードは省略していません。

本報告は、添付原論文・原ソースに照合した是正版の現状を記録します。前回の全対／半径探索によるFilterReg代用を正当化するものではありません。

## 完了条件別の状態

| ID | 状態 | 根拠と限定 |
|---|---|---|
| D01 | PASS | 厳格レビュー、固定作業書、添付資料のSHA256と式・コード・試験対応表を作成しました。 |
| D02 | PASS | 両言語に実permutohedral格子を実装。標準Blur・観測専用no-blur・probreg切替を分離しました。数値比較の実行証拠はC++のみであり、Rust実行はD08の免除対象です。 |
| D03 | PASS | 逆向き事後、分散、2D/3D twist、点対点・線・面を実装。有限差分、式照合、目的関数減少の実行検証はC++です。 |
| D04 | PASS | 直接CPD事後、階乗基底、無正則化SVD、原次数計画、逐次合成、最良状態返却を実装。原コード成分比較はC++実行です。 |
| D05 | PASS | 剛体姿勢固定、写像再適用、ヤコビアン、保存復元をC++数値核＋共通Python層で検証しました。実接続層は未達です。 |
| D06 | PASS | C++数値核をReleaseビルド。100の単体検査、151のdriver経由試験、ASan/UBSan/リーク検査100項目、クリーン展開再実行が合格しました。 |
| D07 | BLOCKED | 実nanobind拡張の構成時にnanobind未取得で失敗。指定版の取得も失敗したため、コンパイル・インポート・境界13試験は未実行です。driverの成功を代用していません。 |
| D08 | SOURCE_ONLY | Rust数値核とPyO3を独立実装し、26設定・5公開関数・90試験のソースを点検。コンパイル/実行必須はユーザー前指示により免除されていますが、Rust数値成功やPyO3接続成功は主張しません。 |
| D09 | BLOCKED | pixiの分離・統合・文書環境とタスクは記述済み。pixi実行形式がなく実依存解決は未達。pixi.lock/Cargo.lockを生成したとは主張しません。 |
| D10 | PASS | 自己完結日本語LaTeXを3回コンパイルし、24ページを画像化して全ページを目視確認しました。クリッピング・重なりなし、Overfull 0。 |
| D11 | PASS | 正本のみの候補ZIPをクリーン展開し、内部SHA256を全照合、C++と151試験を再実行。最終コードが候補と同一であることをハッシュで確認しました。最終文書・報告のみ更新しています。 |

## 修正の要点

FilterRegは実permutohedral格子の単体探索・重心座標・Splat・全軸Blur・Slice、論文(13)のゼロ拡張を実装しました。
標準版、原FilterRegの観測専用Blur省略版、probregの格子数による自動切替は別方式です。Blur省略版も格子補間であり、半径探索ではありません。
固定分散の観測専用格子は再利用します。2D/3D twist、点対点と点対線・面、モーメントと分散を実装しました。

Analytic-CPDは直接事後、階乗付きTaylor基底、無正則化の絶対写像当てはめ、原コードの剰余配分を含む次数計画、現在点群への逐次合成に戻しました。
隠れた正則化、剛体直交投影、固定参照写像、変位上限、減衰はありません。最大次数10は2Dで66基底、3Dで286基底です。
最良内部残差の点群・分散・写像列を一緒に復元します。行数一致だけで外部の既知対応RMSEを登録に持ち込みません。

非剛体モードはFilterRegの姿勢を一度確定し、解析段階でその回転・並進変数を変更しません。
定数・一次のTaylor項は論文どおり残します。残差から剛体様の動きを完全に排除する一意分解ではありません。
解析段階の初期分散はCPD式が既定で、FilterReg分散継承は明示選択の統合拡張です。

## 実行済み試験と未実行試験

C++の単体検査は100項目です。CTestでは1つの実行形式にまとめており、100個のCTest試験ではありません。
Releaseでも検査を無効化せず、ASan・UBSan・リーク検査でも100項目を実行しました。
共通Python層と試験専用driverを通した試験は151件合格しました。原コード比較84条件はこの151件に重なっており、独立の235件として加算しません。
別のクリーン展開から数値核をビルドし、同じ151件を再実行して合格しました。配布するコードはその候補とハッシュ一致しています。
別CMakeプロジェクトからインストール済み静的ライブラリを読み込む2D/3Dの使用例も実行しました。

Rustの90試験、実nanobind/PyO3境界の13試験、C++対Rustの8試験はソースのみで未実行です。
`tests/test_native_boundary.py`は実拡張を要求し、欠落時の自動スキップやdriverへの置換を許しません。
CI設定を収録していますが、外部CIを起動して合格させたという意味ではありません。

## 原コードとの成分比較

| 対象 | 条件数 | 最大絶対差 |
|---|---:|---:|
| permutohedral出力 | 32 | 9.2058785750e-7 |
| 原FilterRegの単体重心座標 | 2 | 2.4017289968e-7 |
| CPD事後統計 | 6 | 3.5527136788e-15 |
| Taylor基底 | 12 | 5.5511151231e-17 |
| 解析M-stepの更新点 | 8 | 5.9952043330e-15 |
| 次数割当 | 24 | 0（整数一致） |

原probregの格子は単精度、本版は倍精度です。上表は「原格子近似との相違」であり、厳密ガウス和に対する近似誤差ではありません。
比較用実行形式は添付原ソースから独立に抽出・ビルドし、本実装の数値核をリンクしていません。
必要な文字コード変換、移植用の宣言置換、抽出範囲・ハッシュは`oracle_provenance.json`にあります。
原プロジェクトのCUDA/PCL/Windows画面を含む全体ビルド、原論文の全データ・全停止判定・速度の再現ではありません。

## 精度監査では悪化例を除外していません

2D/3D、3種子、3方式、2種類の初期分散の36条件を、既知対応を評価側だけで使って監査しました。
剛体段階より既知対応RMSが改善17、悪化6、1e-8以内で不変13、例外0でした。
6件の悪化は2DのCPD初期分散の2種子×3方式です。
例えば標準格子・2D種子90では、初期0.10933、剛体後0.05664、解析後0.17449へ悪化しました。
標準格子・3D種子91では剛体後0.04799から解析後9.79e-5へ改善しました。
分散継承では、初期状態が内部残差上の最良値となって解析写像を採用しない例もあります。

これらの失敗を隠したり、真値RMSEによる停止へ変更したり、成功例だけ残していません。
内部指標sqrt(d sigma2)の改善は、真の対応誤差の改善を意味しません。非凸性、多項式の階数不足・外挿・折返しの制約は残ります。
全36件の点数、設定、誤差、反復・最良反復、終了理由は`synthetic_accuracy.json`にあります。
時間値はdriver起動・入出力等を含み、論文の数値計算時間と比較できません。

## 環境上の未達証拠

Linux x86_64、GNU C++14.2、Python3.13.5で数値核を検証しました。pixi設定のPython3.12環境での試験ではありません。
実nanobind構成はPythonを検出しましたが`No module named nanobind`で停止しました。
指定版nanobind2.12.0の取得もネットワーク／名前解決の問題で失敗しました。
`pixi install -e cpp`はpixi実行形式がなく開始できませんでした。rustc/cargoも存在しません。
実行したコマンド、戻り値、失敗理由は`environment.json`と各ログに保存しています。
これらはコード欠陥を立証する結果ではありませんが、ビルド可能性を立証する結果でもありません。

過去の失敗試験も`pytest_metadata_first_attempt.log`に保持しています。分散メタデータの厳密浮動小数点比較により12件が失敗し、
世界単位への乗算順に由来する丸め差を8機械精度の相対許容へ修正した後、151件が合格しました。改ざん検出は維持しています。
フォント設定での最初のLaTeX失敗も記録しています。最終版は正常コンパイルし、24ページ全てを画像で確認しました。

## 再実行と配布内容

ルートから`python tools/manage.py core-cpp`、`python tools/manage.py test-core`で、同梱Eigenを用いた数値核試験を実行できます。
実接続の受入は`pixi run -e cpp test-cpp`、Rustは`pixi run -e rust test-rust`、実装間比較を含む統合は`pixi run test`です。
これらpixi操作自体の成功は未確認です。文書は`python tools/manage.py docs`で再生成します。
原出力再生成には`tools/build_reference.py`へ元の添付ZIPの保存先を指定します。
`tools/package_release.py`はビルド物・環境・フォントファイルを除外し、ZIP内に各ファイルのSHA256を記録します。
ライセンスと由来を保持した原Eigen以外、巨大な原プロジェクト一式や元論文PDFは再配布していません。

**完了条件を下げずに判定した結果、数値核・文書・配布検査は実施済みですが、D07とD09が残るため総合受入は未完了です。**
