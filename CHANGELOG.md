# 変更履歴

## 0.3.0 — 2026-09-08

固定したDoDの条件は変更していません。0.2.1で達成した検証範囲を保ったまま、**理論整合の是正**と**高速化**を行った版です。
原コード比較84条件、C++単体100項目、ASan/UBSan/リーク検査は全て合格を維持しています。

### 理論整合の是正（結果が変わります）

| 欠陥 | 症状 | 是正 |
|---|---|---|
| 次数継続と停止規則の矛盾 | 予算を増やすほど低次数で「収束」し、アフィン相当の解を返す（2次元 seed 90、予算440で0.0374） | 現在の次数で収束したら段階を進める。段階全体を終えられるのは最高次数で判定が立ったときだけ |
| 解析段の初期分散が段階と不整合 | `cpd`は正規化σ²≈2/dで点群より広く、剛体姿勢を捨てて1反復目に重心へ潰れる（0.057→0.661）。`filterreg`は逆に小さすぎ、写像を1つも採用せず終了 | 既定を`auto`にし、宣言methodで決定（nonrigid→filterreg、analytic→cpd） |
| 反復予算 55 | 次数10に1反復しか残らず2次元の焼き鈍しが完走しない | 既定 220。継続修正により真の上限として働き、実使用は54〜116反復 |
| 発散状態が「最良」に選ばれる | 支持を失った点が自由に外挿され、σ²は責任度重みでそれを見ない。返り値が1.8×10⁷⁷に到達し、続行すると例外送出 | `divergence_radius`（既定100倍）を超える反復を採用せず`numerical_divergence`で停止。**停止規則であり推定値への制約ではない** |

合成精度監査（54条件、両エンジン）:

| | 0.2.0 | 0.3.0 |
|---|---|---|
| 改善 / 悪化 / 不変 | 17 / 6 / 13（36条件） | **54 / 0 / 0** |
| 最終既知対応RMS 中央値 | 0.0408 | **1.02×10⁻⁷** |
| 最悪値 | 0.348 | **1.63×10⁻⁶** |

### 高速化（数値は不変）

決定的な命令数（callgrind Ir）で計測。詳細と再実行手順は `validation/rerun_2026-09-08/SPEED.md`。

- permutohedral格子をアロケーション不要化: blur **1.98倍**、no-blur **1.75倍**
- M-step を JacobiSVD → BDCSVD → 完全直交分解（COD）: M-step単体で最大 **9.3倍**
- 直接法E-stepの二乗ノルム巻き上げ・行優先化・逆数乗算
- 解析段の合計: 3次元 n=200 で **3.20倍**、n=1000 で 1.74倍

COD は階数不足時も最小ノルム最小二乗解を返し、同じ相対しきい値で階数を報告します。監査データの851回の解法で階数不足は0回でした。
原コード比較のM-step一致度はむしろ **5.9952×10⁻¹⁵ → 3.9968×10⁻¹⁵** に改善しています。

### 追加モード

- `backend="fgt"` と `AnalyticOptions(backend="fgt")`: 明示選択のIFGT近似。既定では使われません。
  格子クラスタリングはO(n)で、被覆半径をh=√(2σ²)に比例させるため、σが小さくなっても精度が破綻しません（相対誤差 5×10⁻⁶）。
  **ただし測定結果は「採用に値しない」です。** FilterReg方向では direct より2.5〜4.3倍、格子より6〜93倍遅く、
  ACPD方向でも direct の 0.06〜0.09倍の速度でした。理由と損益分岐点（n≈7×10⁴）は `SPEED.md` に記載しています。
- `FgtOptions(order, max_clusters, cluster_radius, cutoff_radius)` を公開APIに追加。
- **低ランクM-stepは採用しませんでした。** 参考論文の低ランク化は古典CPDのガウス運動核の固有値急減衰に依拠しますが、
  本実装のTaylor設計行列は数値ランクが常にKと一致し（286/286等）、低ランクではありません。根拠は `SPEED.md` 第4節。

### 常設した計測基盤

- `cpp/bench/bench.cpp`（`-DACPD_BUILD_BENCH=ON`）: 段階・バックエンド・点数・次数を切り替えてJSONを出力
- `tools/benchmark_speed.py`: 掃引、`--ir`で決定的命令数、`--compare`で版間A/B
- pixiタスク `bench` / `bench-ir`

### 試験

C++ 193、Rust数値核 90、Rustエンジン 153、実装間比較 10（5バックエンド×2次元）。
ASan/UBSan/リーク検査は100項目に加え、全バックエンドとACPDの両E-stepを実行して合格。

## 0.2.1 — 2026-09-08

固定したDoDの条件は一切変更していません。0.2.0で環境要因により未達だった項目を、実際にビルド・実行して解消した版です。

### 判定

| ID | 0.2.0 | 0.2.1 | 根拠 |
|---|---|---|---|
| D07 実nanobind境界 | BLOCKED | **PASS** | 拡張をビルド・インポートし、driverを使わない境界13試験を含む164試験が合格 |
| D08 Rust/PyO3 | SOURCE_ONLY（実行免除） | **PASS** | 免除を使わず`cargo test`90件、PyO3経由124件、実装間比較8件を実行 |
| D09 実pixi依存解決 | BLOCKED | **PASS** | default/cpp/rust/docs の4環境を解決し、`pixi.lock`と`rust/Cargo.lock`を生成 |
| その他8項目 | PASS | **PASS** | 本環境で全て再実行し、0.2.0の報告値を再現 |

0.2.0がBLOCKEDだった原因は、ビルドホストにnanobind・pixi・cargoが存在せずネットワークからも取得できなかったことです。
コードの欠陥ではありませんでした。0.2.0の判定と証拠ログは `validation/as_delivered_v0.2.0/` にそのまま保存しています。

### 修正

- `cpp/CMakeLists.txt`: 同梱Eigen 3.3.90の`Transpositions.h`は、存在しないメンバ`derived()`を
  `Transpose<TranspositionsBase<Derived>>`に対して呼び出します。upstream Eigenが3.4.0で修正した欠陥です。
  GCC 15はテンプレート本体を実体化前に診断するため、この潜在欠陥がビルド停止に変わりました。
  第三者ヘッダーを1バイトも改変せず、同梱Eigenを使う経路にのみ`-Wno-template-body`を付与します。
  Eigen 3.4以降を使う場合は`ACPD_USE_SYSTEM_EIGEN=ON`を指定してください。
- `.gitignore`: `validation/**/*.log` を除外対象から外しました。DoDの証拠だからです。

数値アルゴリズム、公開API、既定値、停止条件はいずれも変更していません。

### 実行環境

Linux x86_64 / pixi 0.76.2 / Python 3.12.14 / conda-forge GCC 15.3.0 / rustc 1.92.0-nightly /
nanobind 2.12.0 / PyO3 0.28.3。macOS・Windowsは設定のみで未実行です。

## 0.2.0

論文・添付原実装との照合による是正版。詳細は `docs/review/REVIEW.md` と
`validation/as_delivered_v0.2.0/VALIDATION.md` を参照してください。
